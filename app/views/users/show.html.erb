<h3>Page of <%= @user.email %></h3>
<%= link_to 'Edit', edit_user_path(current_user), class:"btn btn-warning" %>
<%= link_to 'Delete', user_path(current_user), class: 'btn btn-danger', method: :delete, data: { confirm: 'Are you sure?' } %>
<h3>Voici la page des membres du club</h3>
<%= link_to "Par ici coquin", secret_path %>
